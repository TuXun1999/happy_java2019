package Datatype;

public class TestCom {
	public static <T extends Comparable<T>> T greatest(T x, T y, T z){
		T tmp;
		if (x.compareTo(y) > 0){
			tmp = x;
		}
		else
			tmp = y;
		
		if (tmp.compareTo(z) > 0){
			return tmp;
		}
		else
			return z;
			
	}

}
